# Runtime deps for the deployed Parquet/DuckDB dashboard (app/streamlit_parquet.py).
# Kept minimal so Streamlit Community Cloud builds fast (no torch/sklearn/etc).
# For the full extraction pipeline / NLP / SQLite alignment page, also install
# requirements-dev.txt.

streamlit>=1.40.0     # dashboard UI
duckdb>=1.1.0         # queries Parquet locally or from S3 (httpfs)
pandas>=2.1.0         # dataframes
plotly>=5.24.0        # charts
networkx>=3.2         # network graphs