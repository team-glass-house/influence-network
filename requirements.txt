# Local dev / full pipeline dependencies (NOT installed on Streamlit Cloud).
# The deployed Parquet app (app/streamlit_parquet.py) only needs the small set
# in requirements-runtime.txt. Install both when running the extraction
# pipeline, notebooks, or the SQLite app's lobbying<->bill alignment page:
#     pip install -r requirements-runtime.txt -r requirements.txt

# Data extraction
requests>=2.31.0
python-dotenv>=1.0.0
tenacity>=8.2.0        # retry/backoff for flaky APIs
lxml>=5.0.0           # fast IRS 990 XML parsing
tqdm>=4.66.0          # progress bars for bulk pulls

# NLP / alignment (analysis/align.py, pdf notebooks)
sentence-transformers>=3.0.0  # semantic text embeddings (pulls in torch)
numpy>=1.26.0
scikit-learn>=1.6.0   # TF-IDF cosine similarity backend
pypdf>=6.1.0          # PDF text extraction
pymupdf>=1.26.0       # fitz - sample PDFs for the similarity demo

# Uploading Parquet to S3 (scripts/upload_parquet_s3.py, inspect scripts)
boto3>=1.34.0
